#! /usr/bin/env bash

# Installs applications.

install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"

# sdkman
curl -s "https://get.sdkman.io" | bash

# Foxit Pahntom reader
echo "Manually install Foxit Phantom Reader"

